port: 7880
bind_addresses:
  - "127.0.0.1"

rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 50100
  use_external_ip: true

keys:
  YOUR_API_KEY: YOUR_API_SECRET

# TURN disabled when nginx uses port 443 for TLS. Enable with external coturn if needed.
turn:
  enabled: false
