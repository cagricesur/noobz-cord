namespace NC.Models
{
    public enum UserRoleEnum
    {
        Member,
        Moderator,
        Admin
    }
    public enum UserStatusEnum
    {
        Passive,
        Pending,
        Active
    }

    public enum TokenStatusEnum
    {
        Passive,
        Expired,
        Active
    }
}
